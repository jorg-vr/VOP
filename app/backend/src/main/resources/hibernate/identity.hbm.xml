<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
    <class name="model.identity.Identity" table="IDENTITY">
        <meta attribute="class-description">
            This class contains the account detail.
        </meta>
        <id name="uuid" type="uuid-binary" column="id">
            <generator class="uuid2"/>
        </id>

        <many-to-one name="address" column="address_id" class="model.identity.Address" cascade="all"/>
        <property name="email" column="email" type="string"/>
        <property name="phoneNumber" column="phonenumber" type="string"/>
        <joined-subclass name="model.identity.Company" table="COMPANY">
            <key column="identity_id"/>
            <property name="name" column="name" type="string"/>
            <property name="btwNumber" column="btw_number" type="string"/>
            <property name="bankAccountNumber" column="bank_account_number" type="string"/>
            <joined-subclass name="model.identity.Customer" table="CUSTOMER">
                <key column="identity_id"/>
                <set name="fleets" access="field" table="FLEET"
                     lazy="false" fetch="select">
                    <key column="owner_id" not-null="true"/>
                    <one-to-many class="model.fleet.Fleet" />
                </set>
            </joined-subclass>
        </joined-subclass>
        <joined-subclass name="model.identity.Person" table="PERSON">
            <key column="identity_id"/>
            <property name="firstName" column="first_name" type="string"/>
            <property name="lastName" column="last_name" type="string"/>
        </joined-subclass>

    </class>
</hibernate-mapping>